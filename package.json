{
  "name": "uso_esbuild",
  "version": "1.0.0",
  "description": "Instalacion de esbuild y primeras pruebas",
  "main": "index.js",
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --outfile=public/bundle.js --define:process.env.NODE_ENV='production'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.8.53"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
